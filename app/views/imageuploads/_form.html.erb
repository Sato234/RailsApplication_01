<%= form_for@imageupload, :html => { :multipart => true } do |f| %>
  <% if @imageupload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imageupload.errors.count, "error") %> prohibited this imageupload from being saved:</h2>

      <ul>
      <% @imageupload.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :uploadedimage %><br />
    <%= f.file_field :uploadedimage %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
